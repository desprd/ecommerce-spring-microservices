server:
  port: 8090
spring:
  application:
    name: "order"
  datasource:
    url: "jdbc:postgresql://localhost:5431/orderdb"
    username: admin
    password: admin
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure:
        include: "*"

