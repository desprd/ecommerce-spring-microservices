server:
  port: 8080
spring:
  application:
    name: "catalog"
  datasource:
    url: "jdbc:postgresql://localhost:5432/catalogdb"
    username: admin
    password: admin
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure:
        include: "*"
